# System map

Where each mechanic lives, who reads it, and what pins it. Read the row you need, then the brief; do not sweep the
repository. Search by symbol, not line number. `codex-original/` is the historical Next.js port and is out of scope.

**Shape.** `Simulation` (`src/game/sim.ts`) is the headless, deterministic, 120 Hz owner of every rule. `Arena`
(`src/game/arena.ts`) subclasses it to add pointer/keyboard input, the fixed-step frame loop with hit-stop, audio cue
diffing and `notify(snapshot)`. React (`src/App.tsx`, `src/ui/*.tsx`) only sends commands and renders `Snapshot`;
`src/game/render.ts` reads live `Simulation` fields for the canvas. Tests import the `.ts` sources directly.

## Update order (one `Simulation.update(dt)` step, `status === 'running'` only)

1. Timers and flashes: adrenaline, drain print, reload clock, dash recharge and ready cue.
2. `updateWave(dt)`: `banner -> spawning -> fighting -> clear -> none`, then `openShop()` or `beginWave(n+1)`.
3. Omens count down (`resolveOmen`); fairness clause (low-HP timer) bookkeeping.
4. `updateEnemy(e, dt)` for every live enemy (AI, contact damage, hazards they emit).
5. Attack timing: windup, `release()`, cooldown, attack-ready cue.
6. Dash movement, then walking movement and hero mechanics driven by movement (crank, momentum, Tempo). A stun
   (`stunned > 0`) skips attack execution and walking; a dash already in progress finishes.
6b. `resolveGazes()`: Witness gazes queued during step 4 resolve now, from the settled `combatFacing` and position; then
   delayed-burst `charges` detonate.
7. Ghosts, order markers, bolts (`landBolt`, `stepPierce`), beams.
8. Drill hazard spawner (drill mode only), then dangers resolve (`takeHit`, `drain`).
9. Gold orbs (pickup), then particles and effects decay.

## Systems

| System | Owner (file / symbols) | Commands | State | Phase | Readers | Tests |
| --- | --- | --- | --- | --- | --- | --- |
| Movement / orders | `sim.ts` `move`, `stop`, `cancel`, `dash`, `effectiveMoveSpeed` | `move(p)`, `stop()`, `dash(p)`; `Arena.handleKey` (S, A, E) and pointer | `player`, `destination`, `dashing`, `dashCharges`, `dashCd`, `momentum` | 6 | `render.ts` (`destination`, `dashing`), snapshot `dashCd`/`dashCharges` | `mechanics`, `skirmisher-events` |
| Attack rhythm | `sim.ts` `attack`, `acquire`, `canFire`, `release`, `rollDamage`, `fireAt`, `effectiveWindup`, `effectiveAttackSpeed`; `cancel` bumps `cancelEvent`; `release` writes `lastShot` (`ShotRecord`: id, origin, angle, weapon, sim time) and sets `recoil` from `heroes.ts` `WEAPON_PRESENTATION` (the one table for recoil duration, kick and launch hold; the renderer reads the same table) | `attack(p)`; right-click / A in `Arena` | `target`, `windupLeft`, `cooldown`, `attackOrder`, `fired`, `cancelled`, `lastShot`, `shotId` (presentation only, never read by a rule) | 5 | `render.ts` `drawPlayer` (launch pose from `lastShot` for `hold` seconds then eased back; recoil eased out; anticipation from `rs.draw`), `arena.ts` `shotEvent` / `cancelEvent` cues | `mechanics`, `weapons`, `attack-feel` |
| Weapons / classes | `heroes.ts` `HEROES`, `Hero.profile`, `Weapon`; `sim.ts` `startReload`, `reload`, `firePierce`, `stepPierce`, `tempest`, `landBolt` | `reload()` (R) | `ammo`, `reloadLeft`, `crank`, `heat`, `tempoShots`, `focusStacks`, `latched` | 5, 6, 7 | `render.ts` (`heat`, `crank`, `ammo`, `reloadLeft`), `arena.ts` ping / rack / loaded cues | `weapons`, `expansion`, `skirmisher-events`, `arena-upgrades` |
| Player damage / death | `sim.ts` `hazardDamage`, `absorb`, `hurt`, `drain`, `takeHit`, `contactHit`, `heal`, `die` | none (internal) | `hp`, `shield`, `invulnerable`, `hurtFlash`, `dead`, `lowHpTimer` | 4, 8 | `render.ts` `drawHud` (in-arena vitals: health, shield, gold, dash pips), snapshot `hp` / `shield`, `arena.ts` `hitEvent` | `mechanics`, `roguelike`, harness `damageByWave` |
| Enemy roster / spawn | `sim.ts` `KIND_COST`, `DIFF`, `wavePool`, `budget`, `beginWave`, `drawKind`, `spawnPoint`, `spawnEnemy`, `makeEnemy`, `applyAffix`, `drawAffix`, `splitEnemy`, `wardenCount` | `beginWave(n)` (wave flow only) | `bag`, `spawnQueue`, `spawnTimer`, `affixBag`, `enemies` | 2 | `render.ts` (`enemies`, `spawnQueue`), snapshot `enemiesLeft` | `expansion`, `roguelike`, `reaver` |
| Enemy AI / combat | `sim.ts` `updateEnemy` (per-kind branches), `flee`, `knock`, `damageEnemy`, `blocks`, `kill`, `REAVER` | none | per-enemy fields on `types.ts` `Enemy` (`phase`, `cooldown`, `wobble`, `burn`, `pattern`) | 4 (AI), 7 (bolt hits) | `render.ts` per-kind draw, `arena.ts` kill / block / swing / cut cues | `expansion`, `reaver`, `mechanics` |
| Hazards | `sim.ts` `lineDanger`, `circleDanger`, `mineDanger`, `cloudDanger`, `telegraph`, `spawnDrillHazard`, `MINE_CAP`, `CLOUD_CAP` | none (emitted by enemies and events) | `dangers`, `nextDanger` | 4 (emit), 8 (resolve) | `render.ts` (`dangers`), snapshot `dodged` | `mechanics`, `expansion` |
| Wave flow / events | `sim.ts` `updateWave`, `scheduleEvents`, `scheduleEvent`, `fireEvent`, `announce`, `resolveOmen`, `takeTithe`, `farPoint`, `EVENT_LABEL` | none | `wave`, `waveState`, `waveTimer`, `fightTime`, `pendingEvents`, `omens`, `eventBanner`, `cullIds` | 2, 3 | `render.ts` (`waveState`, `eventBanner`, `enrage`), snapshot `event` / `cull` / `enrageIn` | `skirmisher-events`, `roguelike`, `economy` |
| Draft / shop / economy (the refit) | `sim.ts` `openShop`, `rollOffers`, `drawAugment`, `drawRarity`, `resolveRarity`, `toOffer`, `rollAnvil`, `choose`, `reroll`, `buyHeal`, `buyShard`, `buyFourth`, `toggleBanish`, `banish`, `ascend`, `continueWave`, `shopEvery`, `refitAfter(wave)` (the one place the cadence rule is written; `updateWave` calls it), `nextShopWave`, `nextRefitWave`, `healAmount`, `anvilPreview`, `recomputeStats`; `upgrades.ts` `UPGRADES`, `eligible`, `offerTier`, `computeStats`, `SHARD_POOL`, `PRISMATIC_CAP` | `choose(i)`, `reroll()`, `buyHeal()`, `buyShard(i)`, `buyFourth()`, `toggleBanish()`, `banish(i)`, `ascend()`, `continueWave()`; keys 1-4, 7-9, R, H, F, B, A, C, Enter (depart: only from the depart button or a non-control target) | `status === 'choosing'`, `draftClaimed`, `claimedOffer`, `offers`, `offerTier`, `anvil`, `gold`, `relics`, `shards`, `rarityBag`, `firstPrismaticWave`, `dryShops` (counts shops, not waves), `banished`, `ascendNext` | 2 (opens on `clear -> none`) | `ui/ShopOverlay.tsx` (presentation only: two tabs Augment / Workshop, disclosures "More draft controls" and "Plan next refit", key-reveal listener that never charges) via snapshot prices and previews (`rerollCost`, `healCost`, `healAmount`, `anvilCost`, `anvilPreview`, `fourthCost`, `banishCost`, `ascendCost`, `nextWave`, `nextShopWave`, `nextRefitWave`); `render.ts` HUD "REFIT AFTER WAVE N" and the clear banner (`refitAfter`); `App.tsx` arena header; `webmcp.ts`; `arena.ts` `buyEvent` cue | `intermission` (incl. cadence 3 pins), `economy` (seeded pins), `arena-upgrades`, `roguelike`, `tuning` (Refit 3 preset, guarantee at cadence 3, `offerTier` sequence record) |
| Quests | `upgrades.ts` `activeQuest`; `sim.ts` `advanceQuest` | none | `questProgress`, `questDone` | 4, 7 (on kill / crit / dodge) | snapshot `questProgress` / `questNeed`, `arena.ts` `questEvent` | `arena-upgrades`, `roguelike` |
| Gold orbs | `sim.ts` `kill` (drop), orb loop in `update` | none | `orbs`, `gold` | 9 | `render.ts`, `arena.ts` `pickupEvent` | `economy`, `roguelike` |
| Facing / crowd control (brief 03) | `sim.ts` `combatFacing`, `faceTarget`, `facingVector`, `FACING_DEADZONE`, `applyStun`, `stunned`, `stunImmune`, `stunSource`, `WITNESS.stunImmunity` | set by `move` (ordered point, dead zone), `attack` and acquisition (live target), `dash` (actual travel); `stop` keeps it | `combatFacing`, `stunned`, `stunImmune`, `stunSource` | 1 (timers), 5-6 (blocks execution) | `render.ts` chevron, LOOK AWAY / SAFE cue, stun ring; snapshot `combatFacing` / `stunned` / `stunSource`; `arena.ts` stun cue | `witness` |
| Witness (brief 03) | `sim.ts` `WITNESS`, `witnessDebut`, `witness` branch of `updateEnemy`, `gazeQueue`, `gazeGeometry`, `gazing`, `gazeExposed`, `resolveGazes`, `lastGaze`, `fixture`; `knock` immunity while winding up; one-per-wave cap in `beginWave`; `scheduleEvents` holds the debut wave | `fixture(name)` (dev) | `Enemy.gazePhase` / `gazeTimer`, `gazeQueue`, `lastGaze`, `sandbox` | 4 (queue), 6b (resolve) | `render.ts` witness draw and `drawEyeMarker`; `arena.ts` gaze / release cues; `ui/tuning.tsx` gaze readout and fixtures; knobs `witness*` | `witness` |
| Next-wave contract (brief 04) | `sim.ts` `contractOffer`, `selectContract`, `selectedContract`, `activeContract`, `contractMult` (applied once inside `hazardDamage`), settlement in the `updateWave` clear branch, `contractReceipt`; `die` voids | `selectContract(id or null)` (key C) | `selectedContract`, `activeContract`, `contractReceipt`, `contractsSigned` | 2 (settle), 4 and 8 (damage) | `ui/ShopOverlay.tsx` contract strip and receipt, `render.ts` badge, `App.tsx` metric tag; knobs `overdraw*` | `contract` |
| Arsenal / account (brief 05) | `arsenal.ts` `CATALOGUE`, `Account`, `purchaseCase`, `acknowledgeReveal`, `buyDirect`, `settleRun`, `equipItem`, `loadoutFor`, `loadoutReadout`, `validateAccount`, `grantLegacyUnlocks`, `nextCaseOdds`; `sim.ts` `setLoadout`, `runLoadout`, `magazineSize`, `reloadTime`, `crankDistance`, `pierceCount`, `punch`, `spooled`, `charges`, `wavesCleared`, `wardenWavesCleared`; `heroes.ts` `isUnlocked(id, { owned })` | `setLoadout(mods, ids)` before `start()`; account writes only through `ui/useAccount.ts` `commit` | localStorage `orbwalk-rogue-account` (plus `.backup`, `.corrupt`); sim `runLoadout` frozen at start | outside the sim (React); 5-7 for loadout effects | `ui/Arsenal.tsx` (workbench, cases, reveal), `App.tsx` settlement on death and the sandbox rule, `render.ts` `drawWeapon` finishes; knobs `credits*`, `caseCost`, `chaseOdds`, `signatureOdds` | `arsenal`, `weapons` (legacy grant) |
| Order markers / cursor | `sim.ts` `orders`, `mark`; `arena.ts` `confirm`, `confirmAttack`, `RenderState.cursorPulse` | pushed by the Arena on real clicks and one-key A, never on right-drag | `orders` | 7 (decay) | `render.ts` `drawOrders`, `drawCursor` | `mechanics` (input path) |
| Dev mode / playtest notes | `App.tsx` `devMode` (`orbwalk-rogue-dev`, `?dev`); `ui/Notes.tsx` `snapshotLine`, `appendNote`, `NoteDialog`; `vite.config.ts` `playtestNotes` (POST `/__playtest-note` appends to `design/PLAYTEST-NOTES.md`); `ui/tuning.tsx` fixtures | key N (`Arena.onNote`) | localStorage `orbwalk-rogue-notes` mirror | n/a | future sessions read `design/PLAYTEST-NOTES.md` | none |
| Tuning | `tuning.ts` `Tuning`, `DEFAULT_TUNING`, `TUNING_KNOBS`, `TUNING_PRESETS` (Ledger, Iron, Easy, and the labelled experiment `iron3` "Refit 3" = Iron with `shopEvery: 3`, `experiment: true`), `EXPERIMENT_GROUPS`, `isExperimentKnob`, `tuningDiff`, `easyBlend`, `normaliseTuning`, `rarityBag`, `scaleAugments`, `kiteMargins`; `sim.ts` `setTuning` (experiment knobs are absolute values and do not count toward preset matching) | `setTuning(patch)` | `tuning` (read at use sites, never copied into entities) | all | `ui/tuning.tsx` panel, `App.tsx` FEEL picker (Iron / Easy / Refit 3 · experiment), `render.ts` | `tuning`, `tests/ab-report.mjs` |
| Cosmetics (particles, effects, shake) | `sim.ts` `burst`, `puff`, `ring`, `effect`, `MAX_PARTICLES`; all randomness via `fx` | none | `particles`, `effects`, `beams`, `ghosts`, `shake`, `impact`, `flash` pulses | 9 | `render.ts` only | `determinism` (particles on/off gives identical gameplay) |
| Visual identity (style contract) | `design/STYLE.md`; `render.ts` `MACHINE` materials, `drawPlayer` casing torso and head, bow branch of `drawWeapon`, Reaver branch of `drawEnemy` + `drawSlabBlade` (pivot-fixed blade whose tip stays inside the wedge), `drawProofInsets`; `sim.ts` `fixture('style')` and the `proof` flag; `arena.ts` `saveFrame(name, region?)` with the `styleFrames` dev middleware in `vite.config.ts` (POST `/__style-frame` writes `design/style/<name>.png`) | dev only: Style proof fixture button (`ui/tuning.tsx`), `__orbwalk.saveFrame` | `proof` (reset by `start()`) | n/a (draw only) | `ui/tuning.tsx`; future sessions read `design/STYLE.md` and `design/style/*.png` | `attack-feel` (fixture contents; `draw()` is read-only under a fake context) |
| RNG / determinism | `math.ts` `Rng` (`seed` is public); `sim.ts` `rng = new Rng(7)` at construction, not reseeded by `start()`; `fx = new Rng(rng.seed ^ FX_SEED_SALT)` in `start()` for cosmetics only | tests set `sim.rng.seed` before `start()` (`tests/harness.mjs`) | `rng.seed`, `fx.seed` | all | none | `economy` pins depend on the exact gameplay draw sequence; `determinism` pins the split |
| Frame loop / input / audio | `arena.ts` `frame`, `schedule`, `handleKey`, `point`, `configure`, `pause`, `togglePause`, `destroy`; `hotkeys.ts` `browserChord`; `audio.ts` `Synth` (`shot` for the bow is a dry transient; `cancelTick`) | `configure(settings)`, `start()`, `pause()`, `togglePause()` | `acc`, `hitStop`, `rs: RenderState` (incl. `draw`, the eased anticipation, and `reducedMotion` = shake off or the OS preference), `*Event` counters on `Simulation` | outside the sim (wraps steps 1-9) | `render.ts` `draw(sim, canvas, ctx, alpha, rs)` | `arena-upgrades` (Arena draft), `hotkeys` |
| Persistence | `App.tsx` `BEST_KEY`, `PROGRESS_KEY`, `TUNING_KEY`, `EASY_KEY` via `loadJson` / `saveJson`; `heroes.ts` `Progress`, `isUnlocked` | none in the sim | localStorage only; the sim holds no persistent state | on `status` change notify | `App.tsx` | `expansion` (unlock rules) |
| WebMCP / trainer | `webmcp.ts` `registerTrainerTools` | wraps `start`, `pause`, `togglePause`, `choose`, `reroll`, `buyHeal`, `buyShard`, `snapshot` | none | n/a | external agents | none |

## Integration checklist

**New enemy:** `EnemyKind` in `types.ts`; `KIND_COST`, the colour table and the `wavePool` debut in `sim.ts`; a stats
branch in `spawnEnemy`; a named `phase` on `Enemy` if it has states (see `gazePhase`); behaviour in `updateEnemy`; a draw
branch in `render.ts`; an optional cue counter diffed in `Arena.frame`; a test in `tests/expansion.test.mjs` or its own
file. Anything that reads the player's facing uses `combatFacing` (never `aimAngle` or the cursor) and resolves after
step 6 the way the gaze queue does.

**New arsenal item:** an entry in `arsenal.ts` `CATALOGUE` with a stable id and `mods`; the sim reads `runLoadout` at the
use site (never the hero definition); a row in `loadoutReadout` if it changes a play unit; a pin in
`tests/arsenal.test.mjs`. Retired ids stay in `validateAccount`'s `retired` list, never silently dropped.

**New shop command:** a method on `Simulation` guarded by `status === 'choosing'`, gold and (if it edits the draft)
`!draftClaimed`; its price and any preview exposed through `snapshot()`; `buyEvent++` if it is a purchase; a key in
`Arena.handleKey`; a button in `ui/ShopOverlay.tsx` calling `g?.command()`; an optional `webmcp.ts` action; a pin in
`tests/intermission.test.mjs`, then re-run `tests/economy.test.mjs`.

## Validation

`npm test` (node:test, 185 tests at 2026-09-06), `npm run typecheck`, `npm run build`, `npm run ab A B runs wave`.
Dev mode in the browser: the Dev mode toggle (or `?dev`) shows the tuning panel with the experiment groups, the stage
fixtures (Witness solo, Witness + Reaver, Range, Style proof) and the note button; N writes a playtest note to
`design/PLAYTEST-NOTES.md` through the dev server.
CI (`.github/workflows/deploy.yml`) runs test and build on push to `main`, then deploys `dist/` to GitHub Pages at 1v5.dev.
